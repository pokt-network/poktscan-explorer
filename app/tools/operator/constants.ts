export const selectedTimeParamKey = 'time'
export const selectedTimeCookieKey = 'selected-time-node-running-dashboard'

export enum TimeClaimProofTable {
  Last24h = 'last24h',
  Last48h = 'last48h',
  LastClaimingWindow = 'lastClaimingWindow',
}

export enum OperatorTabs {
  ClaimProof = 'claim_proof',
  Slashing = 'slashing',
  Suppliers = 'suppliers',
}

export const maxAddresses = 10
